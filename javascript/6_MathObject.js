/* MATH OBJECT : contains properties(variables) and methods for mathematical 
                 and constants
----------------------------------------------------------------------*/                  

val = Math.PI; // M capital
val = Math.abs(-5);
val = Math.max(5,15);
val = Math.max(5,15,4,-2,6,0);
val = Math.min(5,1,2,-1,0);
val = Math.round(2.4);    // 2
val = Math.round(2.499);  // 2
val = Math.round(2.5)     // 3
;
val = Math.ceil(4.3);    // 5
val = Math.ceil(4.6);    // 5

val = Math.floor(2.6);   // 2
val = Math.floor(2.3);   // 2
val = Math.sqrt(64);     
val = Math.sqrt(65);

val = Math.pow(8,2);
val = Math.pow(8,3);
val = Math.pow(8,0.5);
val = Math.random(); //gives random no. between 0-1

val = Math.random() * 20;
val = Math.floor(Math.random() * 5); // random no. between 0 to 4 
val = Math.floor(Math.random() * 5 + 1 ); // random no. between 1 to 5 
val = Math.floor(Math.random() * 5) + 20; // random no. above 20 to 24 
val = Math.floor(Math.random() * 5 + 1 ) + 20; // random no. above 21 to 25
val = Math.ceil(Math.random() * 20); // random no. between 0 to 20

console.log(val);














